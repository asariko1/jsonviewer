# JSON Viewer To-Do

## Remaining

Nothing outstanding right now.

## Decided against

- Automatic diff highlighting for Compare mode:
  Considered adding automatic added/removed/changed highlighting between the two sessions. Decided the manual side-by-side + independent search (as shipped) is good as-is -- not worth the added complexity of array-item matching (by index vs. a key field).
